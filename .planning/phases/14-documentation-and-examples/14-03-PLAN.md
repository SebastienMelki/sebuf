---
phase: 14-documentation-and-examples
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "Top-level README lists protoc-gen-krakend as the sixth generator in the generators table"
    - "README has a KrakenD section explaining the gateway config generator with a quick example"
    - "README links to the krakend-gateway example for the full walkthrough"
    - "CLAUDE.md documents protoc-gen-krakend, the krakend proto package, the krakendgen internal package, and the enum types"
  artifacts:
    - path: "README.md"
      provides: "KrakenD generator documentation visible to all users"
      contains: "protoc-gen-krakend"
    - path: "CLAUDE.md"
      provides: "Updated project documentation for Claude Code"
      contains: "protoc-gen-krakend"
  key_links:
    - from: "README.md"
      to: "examples/krakend-gateway/"
      via: "link in Next Steps section"
      pattern: "krakend-gateway"
---

<objective>
Add protoc-gen-krakend documentation to the top-level README and update CLAUDE.md with KrakenD generator information.

Purpose: Per user decision, KrakenD must be documented in both the top-level README (user-facing) and within the example (covered by Plan 02). This plan handles the README and CLAUDE.md updates.

Output: Updated README.md with KrakenD section, updated CLAUDE.md with krakend generator details.
</objective>

<execution_context>
@/Users/sebastienmelki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastienmelki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-and-examples/14-RESEARCH.md
@.planning/phases/14-documentation-and-examples/14-01-SUMMARY.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add KrakenD section to top-level README</name>
  <files>README.md</files>
  <action>
Read the current README.md and make these additions:

1. **Generators table** (line ~32-41): Add protoc-gen-krakend as the sixth row:
```
| `protoc-gen-krakend` | KrakenD API gateway configs with rate limiting, JWT auth, circuit breakers, and caching |
```

Also update the heading from "Five generators" to "Six generators".

2. **Quick setup section** (line ~134-143): Add the krakend install line:
```bash
go install github.com/SebastienMelki/sebuf/cmd/protoc-gen-krakend@latest
```

3. **New KrakenD section** -- add AFTER the "How it works" section (after the OpenAPI yaml line ~131) and BEFORE "Quick setup". Add a new section:

```markdown
## KrakenD API Gateway

Generate KrakenD API gateway configuration directly from your proto definitions:

```protobuf
service UserService {
  option (sebuf.krakend.gateway_config) = {
    host: ["http://users-backend:8080"]
    timeout: "3s"
    rate_limit: {
      max_rate: 100
      client_max_rate: 10
      strategy: RATE_LIMIT_STRATEGY_IP
    }
    jwt: {
      alg: JWT_ALGORITHM_RS256
      jwk_url: "https://auth.example.com/.well-known/jwks.json"
    }
  };

  rpc GetUser(GetUserRequest) returns (User) {
    option (sebuf.http.config) = {
      path: "/users/{id}"
      method: HTTP_METHOD_GET
    };
  }
}
```

sebuf generates a complete, validated KrakenD configuration:

```json
{
  "endpoint": "/api/v1/users/{id}",
  "method": "GET",
  "input_headers": ["Authorization", "X-API-Key"],
  "backend": [{
    "url_pattern": "/api/v1/users/{id}",
    "host": ["http://users-backend:8080"]
  }],
  "extra_config": {
    "auth/validator": { "alg": "RS256", "jwk_url": "..." },
    "qos/ratelimit/router": { "max_rate": 100, "strategy": "ip" }
  }
}
```

Features: rate limiting, JWT authentication, circuit breakers, HTTP caching, concurrent calls, and automatic header/query forwarding. Per-service configs compose into a complete gateway via KrakenD Flexible Config.

See the [KrakenD Gateway Example](./examples/krakend-gateway/) for a full walkthrough.
```

4. **Next steps section** (line ~179-183): Add a link to the krakend example:
```markdown
- **[KrakenD Gateway Example](./examples/krakend-gateway/)** - Generate and compose API gateway configuration
```

5. **Built on Great Tools section** (line ~185-195): Add KrakenD:
```markdown
- **[KrakenD](https://www.krakend.io/)** - High-performance API gateway with Flexible Config
```

Keep the README concise. The KrakenD section should be a teaser that drives users to the example, not a comprehensive reference.
  </action>
  <verify>
Read the updated README.md and confirm:
- Generators table has 6 rows including protoc-gen-krakend
- KrakenD section exists with proto and JSON examples
- Link to examples/krakend-gateway/ is present
- Quick setup includes krakend install command
  </verify>
  <done>
README.md documents protoc-gen-krakend as the sixth generator with a KrakenD section showing proto annotations and generated output, linking to the full example.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with KrakenD generator documentation</name>
  <files>CLAUDE.md</files>
  <action>
Read the current CLAUDE.md and add KrakenD-related documentation:

1. **Project Overview** (top section): Update the list of plugins from 5 to 6, adding:
   - `**protoc-gen-krakend**`: Generates KrakenD API gateway configuration with rate limiting, JWT authentication, circuit breakers, caching, and automatic header/query forwarding

2. **Architecture > Plugin Structure** section: Add:
   - `**cmd/protoc-gen-krakend/**`: KrakenD gateway config generator entry point
   - `**internal/krakendgen/**`: KrakenD generation logic, endpoint building, validation, and extra_config composition
   - `**proto/sebuf/krakend/**`: KrakenD annotation definitions including gateway_config, endpoint_config, RateLimitStrategy and JWTAlgorithm enums

3. **Architecture > Core Components** section: Add a new entry:
   - **KrakenD Generator** (`internal/krakendgen/generator.go`): Generates per-service KrakenD JSON configuration from proto definitions. Reads sebuf.http annotations for routing and sebuf.krakend annotations for gateway features. Supports rate limiting (endpoint + backend), JWT authentication with claim propagation, circuit breaker, HTTP caching, concurrent calls, and automatic header/query string forwarding. Validates route conflicts (duplicate endpoints, static vs parameterized segment conflicts) at generation time.

4. **Architecture > Generated Output Examples** section: Add a KrakenD example:

```json
// KrakenD Gateway Config - Per-service endpoint fragments
{
  "$schema": "https://www.krakend.io/schema/krakend.json",
  "version": 3,
  "endpoints": [{
    "endpoint": "/api/v1/users/{id}",
    "method": "GET",
    "timeout": "3s",
    "input_headers": ["Authorization", "X-API-Key", "X-User"],
    "backend": [{
      "url_pattern": "/api/v1/users/{id}",
      "host": ["http://users-backend:8080"],
      "extra_config": {
        "qos/circuit-breaker": { "interval": 60, "timeout": 10, "max_errors": 3 },
        "qos/http-cache": { "shared": true }
      }
    }],
    "extra_config": {
      "auth/validator": { "alg": "RS256", "jwk_url": "..." },
      "qos/ratelimit/router": { "max_rate": 100, "strategy": "ip" }
    }
  }]
}
```

5. **KrakenD Annotations** section (new, add after the existing annotation sections): Document the KrakenD annotations:

```
**KrakenD Gateway Annotations** - Service and method-level gateway configuration:

```protobuf
service UserService {
  option (sebuf.krakend.gateway_config) = {
    host: ["http://backend:8080"]
    timeout: "3s"
    rate_limit: { max_rate: 100, strategy: RATE_LIMIT_STRATEGY_IP }
    jwt: { alg: JWT_ALGORITHM_RS256, jwk_url: "..." }
    circuit_breaker: { interval: 60, timeout: 10, max_errors: 3 }
    cache: { shared: true }
    concurrent_calls: 2
  };

  rpc GetUser(GetUserRequest) returns (User) {
    option (sebuf.krakend.endpoint_config) = {
      timeout: "5s"  // Override for this endpoint
      cache: { max_items: 500, max_size: 5242880 }
    };
  }
}
```

6. **Annotation Extension Number Registry** table: Add the krakend extensions:

| 51001 | gateway_config | ServiceOptions | KrakenD gateway service config |
| 51002 | endpoint_config | MethodOptions | KrakenD gateway endpoint overrides |

7. **Annotation Enum Types** (new subsection): Document the enum types:

| Enum | Values | Used In |
|------|--------|---------|
| RateLimitStrategy | IP, HEADER, PARAM | RateLimitConfig.strategy |
| JWTAlgorithm | RS256, RS384, RS512, HS256, HS384, HS512, ES256, ES384, ES512, PS256, PS384, PS512, EdDSA | JWTConfig.alg |

8. **Project Structure** section: Add:
   - `**cmd/protoc-gen-krakend/**`: KrakenD gateway config generator entry point
   - `**internal/krakendgen/**`: KrakenD generation logic and comprehensive test suite
   - `**proto/sebuf/krakend/**`: KrakenD annotation proto definitions
   - `**examples/krakend-gateway/**`: Multi-service KrakenD gateway example with Flexible Config

9. **Testing** section: Add:
```bash
# KrakenD golden file tests
go test -v -run TestKrakenDGoldenFiles ./internal/krakendgen/

# KrakenD schema validation (requires krakend CLI)
go test -v -run TestKrakenDSchemaValidation ./internal/krakendgen/
```

Keep all additions consistent with the existing CLAUDE.md style and formatting.
  </action>
  <verify>
Read the updated CLAUDE.md and confirm:
- protoc-gen-krakend listed as 6th plugin in overview
- internal/krakendgen documented in architecture
- KrakenD annotations section with proto example
- Extension numbers 51001/51002 in registry table
- RateLimitStrategy and JWTAlgorithm enums documented
- Testing commands for krakend generator included
  </verify>
  <done>
CLAUDE.md fully documents the KrakenD generator including plugin structure, generated output, annotations with enums, extension numbers, testing commands, and project structure.
  </done>
</task>

</tasks>

<verification>
1. README.md mentions protoc-gen-krakend in generators table, has KrakenD section, links to example
2. CLAUDE.md documents krakendgen architecture, annotations, enums, testing
3. No broken links or references to non-existent files
4. Style consistent with existing README.md and CLAUDE.md patterns
</verification>

<success_criteria>
- Top-level README introduces protoc-gen-krakend as the sixth generator with code examples
- CLAUDE.md provides comprehensive technical reference for the KrakenD generator
- Both files link to the krakend-gateway example
- Documentation covers all KrakenD features: rate limiting, JWT, circuit breaker, caching, concurrent calls
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-and-examples/14-03-SUMMARY.md`
</output>
