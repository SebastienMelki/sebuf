// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: multi_word_oneof.proto

import { create, fromJson, toJson, type MessageInitShape } from "@bufbuild/protobuf";
import { ApiError, ValidationError } from "./errors.js";
import { MultiWordEventSchema } from "./multi_word_oneof_pb.js";
import type { MultiWordEvent } from "./multi_word_oneof_pb.js";

export interface MultiWordOneofServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface MultiWordOneofServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class MultiWordOneofServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: MultiWordOneofServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async testMultiWordEvent(req: MessageInitShape<typeof MultiWordEventSchema>, options?: MultiWordOneofServiceCallOptions): Promise<MultiWordEvent> {
    let path = "/api/v1/events/multi-word";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(toJson(MultiWordEventSchema, create(MultiWordEventSchema, req))),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return fromJson(MultiWordEventSchema, await resp.json(), { ignoreUnknownFields: true });
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}

