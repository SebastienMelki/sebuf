// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: oneof_discriminator.proto

export type FlattenedEventContent =
  | { type: "text"; body: string }
  | { type: "img"; url: string; width: number; height: number };

export interface FlattenedEventBase {
  id: string;
}

export type FlattenedEvent = FlattenedEventBase & FlattenedEventContent;

export interface TextContent {
  body: string;
}

export interface ImageContent {
  url: string;
  width: number;
  height: number;
}

export type NestedEventContent =
  | { kind: "text"; text?: TextContent }
  | { kind: "image"; image?: ImageContent }
  | { kind: "vid"; video?: VideoContent };

export interface NestedEvent {
  id: string;
  content?: NestedEventContent;
}

export interface VideoContent {
  url: string;
  duration: number;
}

export interface PlainEvent {
  id: string;
  text?: TextContent;
  image?: ImageContent;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface OneofDiscriminatorServiceHandler {
  testFlattenedEvent(ctx: ServerContext, req: FlattenedEvent): Promise<FlattenedEvent>;
  testNestedEvent(ctx: ServerContext, req: NestedEvent): Promise<NestedEvent>;
  testPlainEvent(ctx: ServerContext, req: PlainEvent): Promise<PlainEvent>;
}

export function createOneofDiscriminatorServiceRoutes(
  handler: OneofDiscriminatorServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "POST",
      path: "/api/v1/events/flattened",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as FlattenedEvent;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("testFlattenedEvent", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.testFlattenedEvent(ctx, body);
          return new Response(JSON.stringify(result as FlattenedEvent), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/v1/events/nested",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as NestedEvent;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("testNestedEvent", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.testNestedEvent(ctx, body);
          return new Response(JSON.stringify(result as NestedEvent), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/v1/events/plain",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as PlainEvent;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("testPlainEvent", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.testPlainEvent(ctx, body);
          return new Response(JSON.stringify(result as PlainEvent), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

