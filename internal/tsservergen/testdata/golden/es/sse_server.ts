// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: sse.proto

import { create, fromJson, toJson, type MessageInitShape } from "@bufbuild/protobuf";
import { FieldViolation, ValidationError } from "./errors.js";
import { EventSchema, GetStatusRequestSchema, ResourceEventSchema, StatusResponseSchema, StreamEventsRequestSchema, StreamFilteredEventsRequestSchema, StreamResourceEventsRequestSchema } from "./sse_pb.js";
import type { GetStatusRequest, StreamEventsRequest, StreamFilteredEventsRequest, StreamResourceEventsRequest } from "./sse_pb.js";

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface SSEServiceHandler {
  getStatus(ctx: ServerContext, req: GetStatusRequest): Promise<MessageInitShape<typeof StatusResponseSchema>>;
  streamEvents(ctx: ServerContext, req: StreamEventsRequest): ReadableStream<MessageInitShape<typeof EventSchema>>;
  streamResourceEvents(ctx: ServerContext, req: StreamResourceEventsRequest): ReadableStream<MessageInitShape<typeof ResourceEventSchema>>;
  streamFilteredEvents(ctx: ServerContext, req: StreamFilteredEventsRequest): ReadableStream<MessageInitShape<typeof EventSchema>>;
}

export function createSSEServiceRoutes(
  handler: SSEServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/v1/status",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = create(GetStatusRequestSchema, {});

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getStatus(ctx, body);
          return new Response(JSON.stringify(toJson(StatusResponseSchema, create(StatusResponseSchema, result))), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/v1/events",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = create(StreamEventsRequestSchema, {});

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const stream = handler.streamEvents(ctx, body);

          const sseStream = new ReadableStream({
            async start(controller) {
              const reader = stream.getReader();
              const encoder = new TextEncoder();
              try {
                while (true) {
                  const { done, value } = await reader.read();
                  if (done) break;
                  controller.enqueue(encoder.encode(`data: ${JSON.stringify(toJson(EventSchema, create(EventSchema, value)))}\n\n`));
                }
                controller.close();
              } catch (err) {
                controller.enqueue(
                  encoder.encode(`event: error\ndata: ${JSON.stringify({ message: String(err) })}\n\n`),
                );
                controller.close();
              }
            },
          });

          return new Response(sseStream, {
            headers: {
              "Content-Type": "text/event-stream",
              "Cache-Control": "no-cache",
              "Connection": "keep-alive",
            },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/v1/resources/{resource_id}/events",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const pathSegments = url.pathname.split("/");
          pathParams["resource_id"] = decodeURIComponent(pathSegments[4] ?? "");

          const body = create(StreamResourceEventsRequestSchema, {
            resourceId: pathParams["resource_id"],
          });

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const stream = handler.streamResourceEvents(ctx, body);

          const sseStream = new ReadableStream({
            async start(controller) {
              const reader = stream.getReader();
              const encoder = new TextEncoder();
              try {
                while (true) {
                  const { done, value } = await reader.read();
                  if (done) break;
                  controller.enqueue(encoder.encode(`data: ${JSON.stringify(toJson(ResourceEventSchema, create(ResourceEventSchema, value)))}\n\n`));
                }
                controller.close();
              } catch (err) {
                controller.enqueue(
                  encoder.encode(`event: error\ndata: ${JSON.stringify({ message: String(err) })}\n\n`),
                );
                controller.close();
              }
            },
          });

          return new Response(sseStream, {
            headers: {
              "Content-Type": "text/event-stream",
              "Cache-Control": "no-cache",
              "Connection": "keep-alive",
            },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/v1/events/filtered",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body = create(StreamFilteredEventsRequestSchema, {
            eventType: params.get("type") ?? "",
            limit: Number(params.get("limit") ?? "0"),
          });
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("streamFilteredEvents", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const stream = handler.streamFilteredEvents(ctx, body);

          const sseStream = new ReadableStream({
            async start(controller) {
              const reader = stream.getReader();
              const encoder = new TextEncoder();
              try {
                while (true) {
                  const { done, value } = await reader.read();
                  if (done) break;
                  controller.enqueue(encoder.encode(`data: ${JSON.stringify(toJson(EventSchema, create(EventSchema, value)))}\n\n`));
                }
                controller.close();
              } catch (err) {
                controller.enqueue(
                  encoder.encode(`event: error\ndata: ${JSON.stringify({ message: String(err) })}\n\n`),
                );
                controller.close();
              }
            },
          });

          return new Response(sseStream, {
            headers: {
              "Content-Type": "text/event-stream",
              "Cache-Control": "no-cache",
              "Connection": "keep-alive",
            },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

