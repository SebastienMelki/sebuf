// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: get_params.proto

import { create, fromJson, toJson, type MessageInitShape } from "@bufbuild/protobuf";
import { FieldViolation, ValidationError } from "./errors.js";
import { GetItemRequestSchema, GetItemVersionRequestSchema, ItemSchema, UpdateItemRequestSchema } from "./get_params_pb.js";
import type { GetItemRequest, GetItemVersionRequest, UpdateItemRequest } from "./get_params_pb.js";

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface GetParamsServiceHandler {
  getItem(ctx: ServerContext, req: GetItemRequest): Promise<MessageInitShape<typeof ItemSchema>>;
  getItemVersion(ctx: ServerContext, req: GetItemVersionRequest): Promise<MessageInitShape<typeof ItemSchema>>;
  updateItem(ctx: ServerContext, req: UpdateItemRequest): Promise<MessageInitShape<typeof ItemSchema>>;
}

export function createGetParamsServiceRoutes(
  handler: GetParamsServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/v1/items/{item_id}",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const pathSegments = url.pathname.split("/");
          pathParams["item_id"] = decodeURIComponent(pathSegments[4] ?? "");

          const params = url.searchParams;
          const body = create(GetItemRequestSchema, {
            itemId: pathParams["item_id"],
            query: params.get("q") ?? "",
            limit: Number(params.get("limit") ?? "0"),
            includeArchived: params.get("include_archived") === "true",
            tags: params.getAll("tags"),
            sizes: params.getAll("sizes").map(Number),
            ids: params.getAll("ids").map(BigInt),
            flags: params.getAll("flags").map((v) => v === "true"),
          });
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getItem", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getItem(ctx, body);
          return new Response(JSON.stringify(toJson(ItemSchema, create(ItemSchema, result))), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/v1/items/{item_number}/versions/{version}/draft/{draft}",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const pathSegments = url.pathname.split("/");
          pathParams["item_number"] = decodeURIComponent(pathSegments[4] ?? "");
          pathParams["version"] = decodeURIComponent(pathSegments[6] ?? "");
          pathParams["draft"] = decodeURIComponent(pathSegments[8] ?? "");

          const body = create(GetItemVersionRequestSchema, {
            itemNumber: BigInt(pathParams["item_number"]),
            version: Number(pathParams["version"]),
            draft: pathParams["draft"] === "true",
          });

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getItemVersion(ctx, body);
          return new Response(JSON.stringify(toJson(ItemSchema, create(ItemSchema, result))), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/v1/items/{item_number}",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const pathSegments = url.pathname.split("/");
          pathParams["item_number"] = decodeURIComponent(pathSegments[4] ?? "");

          const body = fromJson(UpdateItemRequestSchema, await req.json(), { ignoreUnknownFields: true });
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("updateItem", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          body.itemNumber = BigInt(pathParams["item_number"]);

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.updateItem(ctx, body);
          return new Response(JSON.stringify(toJson(ItemSchema, create(ItemSchema, result))), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

