"""Code generated by protoc-gen-py-client. DO NOT EDIT."""
from __future__ import annotations

import base64
import json
from dataclasses import dataclass, field
from enum import IntEnum
from typing import Any, ClassVar, Mapping
from urllib.error import HTTPError
from urllib.parse import quote, urlencode
from urllib.request import Request, urlopen

@dataclass(frozen=True, slots=True)
class MethodDescriptor:
    name: str
    http_method: str
    path: str
    request_type: type[Any]
    response_type: type[Any]
    path_params: tuple[str, ...] = ()

@dataclass(frozen=True, slots=True)
class ServiceDescriptor:
    name: str
    base_path: str
    methods: dict[str, MethodDescriptor]

class ApiException(RuntimeError):
    def __init__(self, status_code: int, body: str):
        super().__init__(f"HTTP {status_code}: {body}")
        self.status_code = status_code
        self.body = body

def _encode_bytes(value: bytes, encoding: str) -> str:
    if encoding in ("base64", "base64_raw"):
        encoded = base64.b64encode(value).decode('ascii')
        return encoded.rstrip("=") if encoding == "base64_raw" else encoded
    if encoding in ("base64url", "base64url_raw"):
        encoded = base64.urlsafe_b64encode(value).decode('ascii')
        return encoded.rstrip("=") if encoding == "base64url_raw" else encoded
    if encoding == "hex":
        return value.hex()
    raise ValueError(f"unsupported bytes encoding: {encoding}")

def _decode_bytes(value: str, encoding: str) -> bytes:
    if encoding == "base64":
        return base64.b64decode(value.encode('ascii'))
    if encoding == "base64_raw":
        padded = value + "=" * (-len(value) % 4)
        return base64.b64decode(padded.encode('ascii'))
    if encoding == "base64url":
        return base64.urlsafe_b64decode(value.encode('ascii'))
    if encoding == "base64url_raw":
        padded = value + "=" * (-len(value) % 4)
        return base64.urlsafe_b64decode(padded.encode('ascii'))
    if encoding == "hex":
        return bytes.fromhex(value)
    raise ValueError(f"unsupported bytes encoding: {encoding}")

class WidgetState(IntEnum):
    STATE_UNSPECIFIED = 0
    STATE_READY = 1

_WidgetState_TO_JSON: dict[WidgetState, str] = {
    WidgetState.STATE_UNSPECIFIED: "STATE_UNSPECIFIED",
    WidgetState.STATE_READY: "ready",
}
_WidgetState_FROM_JSON: dict[str, WidgetState] = {
    "STATE_UNSPECIFIED": WidgetState.STATE_UNSPECIFIED,
    "ready": WidgetState.STATE_READY,
}

@dataclass(slots=True)
class DeepNest:
    level1: DeepNestLevel1 | None = None

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.level1
        if value is not None:
            result["level1"] = value.to_dict()
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> DeepNest:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "level1" in data:
            raw_level1 = data["level1"]
            if raw_level1 is None:
                kwargs["level1"] = None
            else:
                kwargs["level1"] = DeepNestLevel1.from_dict(raw_level1)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class DeepNestLevel1:
    level2: DeepNestLevel1Level2 | None = None

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.level2
        if value is not None:
            result["level2"] = value.to_dict()
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> DeepNestLevel1:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "level2" in data:
            raw_level2 = data["level2"]
            if raw_level2 is None:
                kwargs["level2"] = None
            else:
                kwargs["level2"] = DeepNestLevel1Level2.from_dict(raw_level2)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class DeepNestLevel1Level2:
    code: str = ""

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.code
        result["code"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> DeepNestLevel1Level2:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "code" in data:
            raw_code = data["code"]
            kwargs["code"] = str(raw_code)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class Empty:
    def to_dict(self) -> Any:
        return {}

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> Empty:
        _ = data
        return cls()

    def is_empty(self) -> bool:
        return True

@dataclass(slots=True)
class EmptyBehaviorHolder:
    metadata_preserve: EmptyMessage | None = None
    metadata_null: EmptyMessage | None = None
    metadata_omit: EmptyMessage | None = None

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.metadata_preserve
        if value is not None:
            result["metadataPreserve"] = value.to_dict()
        value = self.metadata_null
        if value is not None:
            if hasattr(value, 'is_empty') and value.is_empty():
                result["metadataNull"] = None
            else:
                result["metadataNull"] = value.to_dict()
        value = self.metadata_omit
        if value is not None:
            if hasattr(value, 'is_empty') and value.is_empty():
                pass
            else:
                result["metadataOmit"] = value.to_dict()
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> EmptyBehaviorHolder:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "metadataPreserve" in data:
            raw_metadata_preserve = data["metadataPreserve"]
            if raw_metadata_preserve is None:
                kwargs["metadata_preserve"] = None
            else:
                kwargs["metadata_preserve"] = EmptyMessage.from_dict(raw_metadata_preserve)
        if "metadataNull" in data:
            raw_metadata_null = data["metadataNull"]
            if raw_metadata_null is None:
                kwargs["metadata_null"] = None
            else:
                kwargs["metadata_null"] = EmptyMessage.from_dict(raw_metadata_null)
        if "metadataOmit" in data:
            raw_metadata_omit = data["metadataOmit"]
            if raw_metadata_omit is None:
                kwargs["metadata_omit"] = None
            else:
                kwargs["metadata_omit"] = EmptyMessage.from_dict(raw_metadata_omit)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class EmptyMessage:
    def to_dict(self) -> Any:
        return {}

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> EmptyMessage:
        _ = data
        return cls()

    def is_empty(self) -> bool:
        return True

@dataclass(slots=True)
class GetWidgetRequest:
    id: str = ""

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.id
        result["id"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> GetWidgetRequest:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "id" in data:
            raw_id = data["id"]
            kwargs["id"] = str(raw_id)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class NestedShapeEnvelope:
    kind: str | None = None
    circle: NestedShapeEnvelopeNestedCircle | None = None
    rectangle: NestedShapeEnvelopeNestedRectangle | None = None

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.kind
        if value is not None:
            result["kind"] = value
        value = self.circle
        if value is not None:
            result["circle"] = value.to_dict()
        value = self.rectangle
        if value is not None:
            result["rectangle"] = value.to_dict()
        if self.circle is not None:
            result["kind"] = "circle_shape"
        if self.rectangle is not None:
            result["kind"] = "rectangle"
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> NestedShapeEnvelope:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "kind" in data:
            raw_kind = data["kind"]
            if raw_kind is None:
                kwargs["kind"] = None
            else:
                kwargs["kind"] = str(raw_kind)
        if "circle" in data:
            raw_circle = data["circle"]
            if raw_circle is None:
                kwargs["circle"] = None
            else:
                kwargs["circle"] = NestedShapeEnvelopeNestedCircle.from_dict(raw_circle)
        if "rectangle" in data:
            raw_rectangle = data["rectangle"]
            if raw_rectangle is None:
                kwargs["rectangle"] = None
            else:
                kwargs["rectangle"] = NestedShapeEnvelopeNestedRectangle.from_dict(raw_rectangle)
        discriminator_shape = data.get("kind")
        if discriminator_shape == "circle_shape":
            if "circle" in data:
                kwargs["circle"] = NestedShapeEnvelopeNestedCircle.from_dict(data["circle"])
        if discriminator_shape == "rectangle":
            if "rectangle" in data:
                kwargs["rectangle"] = NestedShapeEnvelopeNestedRectangle.from_dict(data["rectangle"])
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class NestedShapeEnvelopeNestedCircle:
    radius: float = 0.0

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.radius
        result["radius"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> NestedShapeEnvelopeNestedCircle:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "radius" in data:
            raw_radius = data["radius"]
            kwargs["radius"] = float(raw_radius)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class NestedShapeEnvelopeNestedRectangle:
    width: float = 0.0
    height: float = 0.0

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.width
        result["width"] = value
        value = self.height
        result["height"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> NestedShapeEnvelopeNestedRectangle:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "width" in data:
            raw_width = data["width"]
            kwargs["width"] = float(raw_width)
        if "height" in data:
            raw_height = data["height"]
            kwargs["height"] = float(raw_height)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class SearchWidgetsRequest:
    owner_id: str = ""
    tag_ids: list[str] = field(default_factory=list)

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.owner_id
        result["ownerId"] = value
        value = self.tag_ids
        result["tagIds"] = [item for item in value]
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> SearchWidgetsRequest:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "ownerId" in data:
            raw_owner_id = data["ownerId"]
            kwargs["owner_id"] = str(raw_owner_id)
        if "tagIds" in data:
            raw_tag_ids = data["tagIds"]
            kwargs["tag_ids"] = [str(item) for item in raw_tag_ids]
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class ShapeEnvelope:
    kind: str | None = None
    radius: float | None = None
    width: float | None = None
    height: float | None = None

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.kind
        if value is not None:
            result["kind"] = value
        value = self.radius
        if value is not None:
            result["radius"] = value
        value = self.width
        if value is not None:
            result["width"] = value
        value = self.height
        if value is not None:
            result["height"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> ShapeEnvelope:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "kind" in data:
            raw_kind = data["kind"]
            if raw_kind is None:
                kwargs["kind"] = None
            else:
                kwargs["kind"] = str(raw_kind)
        if "radius" in data:
            raw_radius = data["radius"]
            if raw_radius is None:
                kwargs["radius"] = None
            else:
                kwargs["radius"] = float(raw_radius)
        if "width" in data:
            raw_width = data["width"]
            if raw_width is None:
                kwargs["width"] = None
            else:
                kwargs["width"] = float(raw_width)
        if "height" in data:
            raw_height = data["height"]
            if raw_height is None:
                kwargs["height"] = None
            else:
                kwargs["height"] = float(raw_height)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class ShapeEnvelopeCircle:
    radius: float = 0.0

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.radius
        result["radius"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> ShapeEnvelopeCircle:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "radius" in data:
            raw_radius = data["radius"]
            kwargs["radius"] = float(raw_radius)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class ShapeEnvelopeRectangle:
    width: float = 0.0
    height: float = 0.0

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.width
        result["width"] = value
        value = self.height
        result["height"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> ShapeEnvelopeRectangle:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "width" in data:
            raw_width = data["width"]
            kwargs["width"] = float(raw_width)
        if "height" in data:
            raw_height = data["height"]
            kwargs["height"] = float(raw_height)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class TagList:
    values: list[str] = field(default_factory=list)

    def to_dict(self) -> Any:
        value = self.values
        return [item for item in value]

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> TagList:
        if data is None:
            return cls()
        return cls(values=[str(item) for item in data])

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class WellKnownHolder:
    any_value: dict[str, Any] | None = None
    ttl: str | None = None
    mask: list[str] | None = None
    items: list[Any] | None = None
    raw_value: dict[str, Any] | None = None

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.any_value
        if value is not None:
            result["anyValue"] = value
        value = self.ttl
        if value is not None:
            result["ttl"] = value
        value = self.mask
        if value is not None:
            result["mask"] = value
        value = self.items
        if value is not None:
            result["items"] = value
        value = self.raw_value
        if value is not None:
            result["rawValue"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> WellKnownHolder:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "anyValue" in data:
            raw_any_value = data["anyValue"]
            if raw_any_value is None:
                kwargs["any_value"] = None
            else:
                kwargs["any_value"] = raw_any_value
        if "ttl" in data:
            raw_ttl = data["ttl"]
            if raw_ttl is None:
                kwargs["ttl"] = None
            else:
                kwargs["ttl"] = raw_ttl
        if "mask" in data:
            raw_mask = data["mask"]
            if raw_mask is None:
                kwargs["mask"] = None
            else:
                kwargs["mask"] = raw_mask
        if "items" in data:
            raw_items = data["items"]
            if raw_items is None:
                kwargs["items"] = None
            else:
                kwargs["items"] = raw_items
        if "rawValue" in data:
            raw_raw_value = data["rawValue"]
            if raw_raw_value is None:
                kwargs["raw_value"] = None
            else:
                kwargs["raw_value"] = raw_raw_value
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class Widget:
    id: str = ""
    display_name: str | None = None
    scores: dict[str, int] = field(default_factory=dict)
    meta: dict[str, Any] | None = None
    created_at: int | None = None
    alias: str | None = None
    state: WidgetState = WidgetState(0)
    meta_note: str | None = None
    tags: list[str] = field(default_factory=list)
    owner_id: str = ""
    payload: bytes = b""
    version: int = 0
    state_labels: dict[str, WidgetState] = field(default_factory=dict)
    profiles_by_id: dict[str, WidgetProfile] = field(default_factory=dict)

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.id
        result["id"] = value
        value = self.display_name
        if value is not None:
            result["displayName"] = value
        value = self.scores
        result["scores"] = {key: item for key, item in value.items()}
        value = self.meta
        if value is not None:
            result["meta"] = value
        value = self.created_at
        if value is not None:
            result["createdAt"] = value
        value = self.alias
        if value is not None:
            result["alias"] = value
        value = self.state
        result["state"] = int(value)
        value = self.meta_note
        if value is not None:
            result["meta_note"] = value
        value = self.tags
        result["tags"] = [item for item in value]
        value = self.owner_id
        result["ownerId"] = value
        value = self.payload
        result["payload"] = _encode_bytes(value, "hex")
        value = self.version
        result["version"] = value
        value = self.state_labels
        result["stateLabels"] = {key: _WidgetState_TO_JSON[item] for key, item in value.items()}
        value = self.profiles_by_id
        result["profilesById"] = {key: item.to_dict() for key, item in value.items()}
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> Widget:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "id" in data:
            raw_id = data["id"]
            kwargs["id"] = str(raw_id)
        if "displayName" in data:
            raw_display_name = data["displayName"]
            if raw_display_name is None:
                kwargs["display_name"] = None
            else:
                kwargs["display_name"] = str(raw_display_name)
        if "scores" in data:
            raw_scores = data["scores"]
            kwargs["scores"] = {str(key): int(item) for key, item in raw_scores.items()}
        if "meta" in data:
            raw_meta = data["meta"]
            if raw_meta is None:
                kwargs["meta"] = None
            else:
                kwargs["meta"] = raw_meta
        if "createdAt" in data:
            raw_created_at = data["createdAt"]
            if raw_created_at is None:
                kwargs["created_at"] = None
            else:
                kwargs["created_at"] = raw_created_at
        if "alias" in data:
            raw_alias = data["alias"]
            if raw_alias is None:
                kwargs["alias"] = None
            else:
                kwargs["alias"] = raw_alias
        if "state" in data:
            raw_state = data["state"]
            kwargs["state"] = WidgetState(raw_state)
        if "meta_note" in data:
            raw_meta_note = data["meta_note"]
            if raw_meta_note is None:
                kwargs["meta_note"] = None
            else:
                kwargs["meta_note"] = str(raw_meta_note)
        if "tags" in data:
            raw_tags = data["tags"]
            kwargs["tags"] = [str(item) for item in raw_tags]
        if "ownerId" in data:
            raw_owner_id = data["ownerId"]
            kwargs["owner_id"] = str(raw_owner_id)
        if "payload" in data:
            raw_payload = data["payload"]
            kwargs["payload"] = _decode_bytes(str(raw_payload), "hex")
        if "version" in data:
            raw_version = data["version"]
            kwargs["version"] = int(raw_version)
        if "stateLabels" in data:
            raw_state_labels = data["stateLabels"]
            kwargs["state_labels"] = {str(key): _WidgetState_FROM_JSON[str(item)] for key, item in raw_state_labels.items()}
        if "profilesById" in data:
            raw_profiles_by_id = data["profilesById"]
            kwargs["profiles_by_id"] = {str(key): WidgetProfile.from_dict(item) for key, item in raw_profiles_by_id.items()}
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

@dataclass(slots=True)
class WidgetProfile:
    note: str = ""

    def to_dict(self) -> Any:
        result: dict[str, Any] = {}
        value = self.note
        result["note"] = value
        return result

    @classmethod
    def from_dict(cls, data: Mapping[str, Any] | None) -> WidgetProfile:
        if data is None:
            return cls()
        kwargs: dict[str, Any] = {}
        if "note" in data:
            raw_note = data["note"]
            kwargs["note"] = str(raw_note)
        return cls(**kwargs)

    def is_empty(self) -> bool:
        return self.to_dict() in ({}, [], None)

SERVICES: dict[str, ServiceDescriptor] = {
    "WidgetService": ServiceDescriptor(
        name="WidgetService",
        base_path="/api/v1",
        methods={
            "GetWidget": MethodDescriptor(
                name="GetWidget",
                http_method="GET",
                path="/api/v1/widgets/{id}",
                request_type=GetWidgetRequest,
                response_type=Widget,
                path_params=("id"),
            ),
            "SearchWidgets": MethodDescriptor(
                name="SearchWidgets",
                http_method="GET",
                path="/api/v1/widgets",
                request_type=SearchWidgetsRequest,
                response_type=Widget,
                path_params=(),
            ),
        },
    ),
    "AdminService": ServiceDescriptor(
        name="AdminService",
        base_path="/api/v1/admin",
        methods={
            "ResetWidget": MethodDescriptor(
                name="ResetWidget",
                http_method="POST",
                path="/api/v1/admin/widgets/{id}:reset",
                request_type=GetWidgetRequest,
                response_type=Empty,
                path_params=("id"),
            ),
        },
    ),
}

class WidgetServiceClient:
    SERVICE: ClassVar[ServiceDescriptor] = SERVICES["WidgetService"]

    def __init__(
        self,
        base_url: str,
        headers: Mapping[str, str] | None = None,
        timeout: float | None = None,
    ):
        self._base_url = base_url.rstrip('/')
        self._headers = dict(headers or {})
        self._timeout = timeout

    def get_widget(
        self,
        request: GetWidgetRequest,
    ) -> Widget:
        descriptor = self.SERVICE.methods["GetWidget"]
        path = descriptor.path
        path = path.replace("{id}", quote(str(request.id), safe=""))
        query: dict[str, Any] = {}
        request_dict = request.to_dict()
        request_dict.pop("id", None)
        body = None
        payload = self._request(descriptor.http_method, path, query, body)
        return Widget.from_dict(payload)

    def search_widgets(
        self,
        request: SearchWidgetsRequest,
    ) -> Widget:
        descriptor = self.SERVICE.methods["SearchWidgets"]
        path = descriptor.path
        query: dict[str, Any] = {}
        request_dict = request.to_dict()
        if "ownerId" in request_dict:
            query["owner"] = request_dict.pop("ownerId")
        if "tagIds" in request_dict:
            query["tag_id"] = request_dict.pop("tagIds")
        body = None
        payload = self._request(descriptor.http_method, path, query, body)
        return Widget.from_dict(payload)

    def _request(self, method: str, path: str, query: dict[str, Any], body: Any) -> Any:
        url = self._base_url + path
        query = {key: value for key, value in query.items() if value is not None}
        if query:
            url += '?' + urlencode(query, doseq=True)
        headers = dict(self._headers)
        data: bytes | None = None
        headers.setdefault("Accept", "application/json")
        if body is not None:
            headers.setdefault("Content-Type", "application/json")
            data = json.dumps(body).encode('utf-8')
        request = Request(url=url, data=data, headers=headers, method=method)
        try:
            with urlopen(request, timeout=self._timeout) as response:
                payload = response.read()
        except HTTPError as exc:
            body_text = exc.read().decode('utf-8', errors='replace')
            raise ApiException(exc.code, body_text) from exc
        if not payload:
            return None
        return json.loads(payload.decode('utf-8'))

class AdminServiceClient:
    SERVICE: ClassVar[ServiceDescriptor] = SERVICES["AdminService"]

    def __init__(
        self,
        base_url: str,
        headers: Mapping[str, str] | None = None,
        timeout: float | None = None,
    ):
        self._base_url = base_url.rstrip('/')
        self._headers = dict(headers or {})
        self._timeout = timeout

    def reset_widget(
        self,
        request: GetWidgetRequest,
    ) -> Empty:
        descriptor = self.SERVICE.methods["ResetWidget"]
        path = descriptor.path
        path = path.replace("{id}", quote(str(request.id), safe=""))
        query: dict[str, Any] = {}
        request_dict = request.to_dict()
        request_dict.pop("id", None)
        body = request_dict
        payload = self._request(descriptor.http_method, path, query, body)
        return Empty()

    def _request(self, method: str, path: str, query: dict[str, Any], body: Any) -> Any:
        url = self._base_url + path
        query = {key: value for key, value in query.items() if value is not None}
        if query:
            url += '?' + urlencode(query, doseq=True)
        headers = dict(self._headers)
        data: bytes | None = None
        headers.setdefault("Accept", "application/json")
        if body is not None:
            headers.setdefault("Content-Type", "application/json")
            data = json.dumps(body).encode('utf-8')
        request = Request(url=url, data=data, headers=headers, method=method)
        try:
            with urlopen(request, timeout=self._timeout) as response:
                payload = response.read()
        except HTTPError as exc:
            body_text = exc.read().decode('utf-8', errors='replace')
            raise ApiException(exc.code, body_text) from exc
        if not payload:
            return None
        return json.loads(payload.decode('utf-8'))

